# Workflows

- **utility-frame_interpolation-film**: FILM VFI frame interpolation (ComfyUI-Frame-Interpolation). Export from ComfyUI Desktop 2026 (Save workflow as JSON) into this folder as `utility-frame_interpolation-film.json`.
